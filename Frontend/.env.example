# Copy this file to .env (not committed — see ../.gitignore) and fill in
# the real values. Both are already fixed per the backend's settings.py:
VITE_MSAL_CLIENT_ID=843d2ee1-95e6-4b03-855b-06d31771f89e
VITE_MSAL_TENANT_ID=0eadb77e-42dc-47f8-bbe3-ec2395e0712c

# Dev-only escape hatch — adds a "dev bypass" option on the Login screen
# that skips real Entra sign-in entirely. Real MSAL login is blocked right
# now on an org-side Entra admin-consent grant, unrelated to this app's
# code (see Login.jsx/App.jsx). Leave this unset/false normally; only
# uncomment locally while that's unresolved. Never set true in a deployed
# build — see Login.jsx's DEV_MOCK_LOGIN check.
# VITE_DEV_MOCK_LOGIN=true
