# No frontend environment variables are needed. Real Entra/MSAL login
# was removed (see Login.jsx/App.jsx/Data/demoAuth.js) — the demo login
# (POST /demo-login) is the only sign-in path now and needs no config.
# This file is kept as a placeholder in case a real env var is needed
# again later.
